# Gradle Properties for PXL Project

# JVM Configuration
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Gradle Daemon
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true

# Java Toolchain
# Use Java 8 for compatibility with legacy code
# If you have JDK 11+ installed, you can change this
# To explicitly set Java home, uncomment and set an absolute path, for example:
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-11.0.28.6-hotspot
# Otherwise, leave this unset to allow Gradle to use the JAVA_HOME environment variable
# or the system JDK found on your PATH.


# Build Configuration
org.gradle.warning.mode=all
org.gradle.console=auto

# Project Configuration
version=1.0.0
group=me.pxl
